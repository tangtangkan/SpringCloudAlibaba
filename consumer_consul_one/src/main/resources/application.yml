server:
  port: 82

spring:
  application:
    name: consumer-consul          # 服务与服务间的调用
  cloud:
    consul:
      host: localhost
      port: 8500
      discovery:
        service-name: ${spring.application.name}